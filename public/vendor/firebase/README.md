Firebase JS SDK 10.14.1 (firebase-app.js + firebase-auth.js), self-hosted.
Internal gstatic module URLs rewritten to /vendor/firebase/.
Update: re-download from https://www.gstatic.com/firebasejs/<v>/ and re-run the sed rewrite.
