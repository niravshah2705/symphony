{
  "name": "ai-fleet-frontend",
  "version": "1.0.0",
  "description": "AI Fleet frontend — the browser SPA (public/) and the Playwright e2e suite. The backend is Python: see ai_fleet/ and pyproject.toml (run `python -m ai_fleet.start_all`).",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:debug": "PWDEBUG=1 playwright test --project=chrome"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  },
  "engines": {
    "node": ">=18"
  }
}
