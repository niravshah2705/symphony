{
  "name": "@ai-fleet/provisioner",
  "version": "1.0.0",
  "description": "AI Fleet provisioner service — internal, IAM-gated; provisions/tears down per-tenant Cloud Run stacks on tenant-provision events.",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=../../.env src/index.js",
    "dev": "node --watch --env-file-if-exists=../../.env src/index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@ai-fleet/shared-core": "*",
    "express": "^5.2.1",
    "google-auth-library": "^11.0.1"
  }
}
