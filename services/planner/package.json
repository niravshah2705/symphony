{
  "name": "@ai-fleet/planner",
  "version": "1.0.0",
  "description": "AI Fleet planner service — isolated software-design planner (deep agent + enrichment scheduler) exposing /api/agent.",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=../../.env src/index.js",
    "dev": "node --watch --env-file-if-exists=../../.env src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@ai-fleet/shared": "*",
    "express": "^4.19.2"
  }
}
